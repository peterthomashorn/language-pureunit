'use babel';

import LanguagePureunitView from '../lib/language-pureunit-view';

describe('LanguagePureunitView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
